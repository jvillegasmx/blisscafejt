# Enable URL rewriting
RewriteEngine On
RewriteBase /

# Don't route index.php to itself (prevent infinite loop)
RewriteCond %{REQUEST_URI} ^/index\.php$ [NC]
RewriteRule ^ - [L]

# Route ALL requests through index.php
RewriteRule ^(.*)$ index.php [QSA,L]
